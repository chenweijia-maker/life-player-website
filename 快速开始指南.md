# 快速开始指南 - Android APP

## ✅ 当前状态

- ✅ Android 平台已添加
- ✅ 依赖已安装
- ✅ 配置文件已就绪

## 🚀 接下来的步骤

### 1. 手动构建前端（由于权限限制）

由于系统权限限制，请手动执行以下操作：

**Windows PowerShell：**
```powershell
# 在项目根目录执行
New-Item -ItemType Directory -Force -Path dist
Copy-Item -Path "frontend\*" -Destination "dist\" -Recurse -Force
```

**或者使用文件管理器：**
1. 创建 `dist` 文件夹
2. 将 `frontend` 文件夹中的所有文件复制到 `dist` 文件夹

### 2. 同步 Capacitor 资源

```bash
npm run cap:sync
```

### 3. 启动后端服务器

**重要：必须先启动后端！**

```bash
npm start
```

后端会在 `http://localhost:4000` 运行

### 4. 查找你的电脑 IP 地址

**Windows：**
```bash
ipconfig
```
查找 "IPv4 地址"，例如：`192.168.1.100`

### 5. 配置后端服务器地址

编辑 `capacitor.config.js`，将 `server.url` 改为你的电脑 IP：

```javascript
server: {
  url: 'http://192.168.1.100:4000',  // 替换为你的实际 IP
  cleartext: true
}
```

### 6. 在 Android Studio 中打开项目

```bash
npm run cap:open:android
```

### 7. 运行应用

- **在模拟器中**：创建/启动 Android 虚拟设备，点击运行
- **在真机上**：启用 USB 调试，连接手机，点击运行

## 📱 构建 APK 安装包

如果想直接安装到手机：

1. 在 Android Studio 中选择 "Build" > "Build Bundle(s) / APK(s)" > "Build APK(s)"
2. APK 位置：`android/app/build/outputs/apk/debug/app-debug.apk`
3. 将 APK 传输到手机并安装

## ⚠️ 重要提示

1. **必须先启动后端服务器**：`npm start`
2. **Android 设备不能使用 localhost**：必须使用电脑的局域网 IP
3. **确保手机和电脑在同一网络**
4. **每次修改前端代码后**：需要重新构建和同步
   ```bash
   # 复制文件到 dist
   Copy-Item -Path "frontend\*" -Destination "dist\" -Recurse -Force
   # 同步到 Android
   npm run cap:sync
   ```

## 📚 详细文档

- `ANDROID_BUILD.md` - 完整构建指南
- `README_ANDROID.md` - Android 快速开始
- `如何查看产品_Android版.md` - 详细使用说明

## 🆘 遇到问题？

1. 检查后端是否运行：`npm start`
2. 检查 IP 地址是否正确
3. 查看 Android Studio 的 Build 输出
4. 检查网络连接
